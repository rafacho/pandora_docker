# colcon build artifacts (generated inside the container's
# ~/ros_lyrical_docker_ws, or accidentally at the project root if colcon is
# ever run outside Docker against a host ROS install)
build/
install/
log/

# Python
__pycache__/
*.pyc

# Persisted zsh command history (host-side, see docker-compose.yml)
.docker-data/

# Claude Code local/session-specific settings (not shared config)
.claude/settings.local.json

src/jacobian.pdf
src/velocity_analysis.pdf
src/pandora_control/pandora_control/validation_tests/